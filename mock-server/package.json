{
  "name": "ecommerce-mock-server",
  "version": "1.0.0",
  "description": "Mock server for ecommerce API",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}